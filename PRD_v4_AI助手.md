# 增量 PRD v4.0 — AI 助手模块（A 本地问答 + B 对话式 AI）

> 版本：v4.0（第四轮增量）｜作者：Alice（PM）｜状态：待架构师评审
> 仓库：chenliguan42057/outbound-registry｜线上：https://chenliguan42057.github.io/outbound-registry/
> 基线：remote main = 2e35b677（第三轮已上线）

---

## 0. 项目信息

- **Language**：中文（与用户需求一致）
- **技术栈**：沿用现有经典 `<script>` 顺序加载（非 ES Module），**不引入任何第三方依赖**；AI 调用一律原生 `fetch`
- **文件结构建议**（架构师可调整）：
  - `src/js/ai/engine.js` — A 部分本地问答引擎（意图识别 + 数据查询 + 回复构建）
  - `src/js/ai/llm.js` — B 部分 API 对接（fetch / CORS / 错误处理 / 超时）
  - `src/js/ai/chat.js` — 聊天面板 UI 组件（气泡 / chips / 输入 / 状态 / 清空 / 设置弹窗）
  - `src/js/views/ai.js` — 管理后台「AI 助手」页面挂载（#/app/ai）
  - `src/css/ai.css` — AI 面板与浮动气泡样式（沿用现有设计令牌，建议独立文件便于维护）
- **原始需求复述**：为进销存系统新增「AI 助手」：A 部分免费零配置的本地智能问答（基于系统已有数据做自然语言查询，纯前端规则匹配，不调外部 API）；B 部分可选的对话式 AI（用户自填 API Key 存 localStorage，接入支持浏览器直连的大模型如 DeepSeek，无 Key 自动退化为本地问答）。入口为落地页右下角浮动气泡 + 管理后台侧栏菜单项（#/app/ai）。

---

## 1. 增量目标

### A 部分（核心，免费零配置）
1. **零配置自然语言查询**：用户无需任何配置/联网，即可用自然语言查询库存、出入库统计、记录、排行趋势。
2. **覆盖高频场景**：单品库存、低库存预警、今日/近 N 天出入库统计、记录检索（领取人/部门/用途）、排行与趋势 ≥ 6 类查询。
3. **纯前端实现**：规则匹配 + 关键词解析 + 数据查询，**不调用任何外部 API**，离线可用、零成本、部署即上线。
4. **复用现有数据层**：仅读 `State.list` + `Stock.getStock/summarize/trend` + `Config.PRODUCTS/INVENTORY`，不新增数据源、不改记录 schema。
5. **回复可视化 + 移动端友好**：表格/列表/文本多样式渲染，现场手机可用。

### B 部分（升级，可选）
1. **可选接入大模型**：设置中填写 API Key 即升级为真对话模式（可闲聊、可结合系统数据上下文回答）。
2. **OpenAI 兼容接口**：默认 DeepSeek（https://api.deepseek.com，`deepseek-chat`，浏览器直连支持 CORS）。
3. **智能切换**：无 Key → 纯本地引擎；有 Key → 混合模式（数据类问题优先本地引擎保准确+零成本，闲聊/未命中走 LLM）。
4. **Key 安全**：仅存本机浏览器 localStorage，**绝不**入云端同步、不入仓库、不入代码。
5. **错误处理完备**：Key 无效 / 限流 / 超时 / 断网均有友好提示，并自动回退本地引擎。

---

## 2. 变更详述

### 2.1 AI 助手整体架构（A + B 切换逻辑）

```
用户输入
   │
   ▼
┌─────────────────────────────┐
│ chat.js 聊天面板             │  ← 单例浮动面板，落地页 & 管理后台共用
└──────────────┬──────────────┘
               ▼
┌─────────────────────────────┐
│ engine.js 意图识别           │
│ ① 归一化（去标点/小写/trim）  │
│ ② 产品名模糊匹配（PRODUCTS）  │
│ ③ 意图关键词匹配             │
└──────────────┬──────────────┘
               │
        ┌──────┴──────┐
        ▼             ▼
  命中数据意图       未命中（闲聊/模糊）
        │             │
        ▼             │
┌──────────────┐      │
│ 本地数据查询   │      │
│ （A 核心）    │      │
└──────┬───────┘      │
       │              │
       ▼              ▼
┌─────────────────────────────┐
│ 决策：是否有有效 API Key？     │
│  · 无 Key → 直接返回本地结果   │
│  · 有 Key → 数据意图直接返回   │
│   本地结果（准确+免费）；       │
│   未命中 → 调 llm.js 真对话   │
└─────────────────────────────┘
```

- **无 Key（默认，A 模式）**：全部走本地引擎，100% 离线可用。
- **有 Key（B 模式）**：混合策略——数据类意图仍由本地引擎回答（零成本、准确、快）；闲聊/未命中类问题携带「系统数据上下文」调 LLM 真对话。若 LLM 调用失败（Key 失效/断网/超时），若该问题仍疑似数据意图则回退本地引擎并附提示，否则给出友好错误文案。
- **架构约定**：`engine.js` 必须可独立运行（不依赖 Key）；`llm.js` 仅在配置了 Key 时被调用；`chat.js` 为通用聊天 UI，落地页与管理后台复用同一套。

### 2.2 A 部分支持的查询类型清单

| # | 类型 | 示例问句 | 返回内容说明 |
|---|------|----------|--------------|
| 1 | **单品/多品库存查询** | 「冻干精华液 20支装 还有多少？」「面膜还有多少？」 | 命中货品的当前库存（文本或单行列表）；未命中提示最相近货品名 |
| 2 | **低库存预警** | 「哪些货品低库存？」「什么快没货了？」 | 低于阈值（Config.LOW_STOCK_THRESHOLD=30）的货品表格（名称+当前库存），空时提示「暂无低库存货品」 |
| 3 | **今日/日期出入库统计** | 「今天出了多少货？」「昨天入了多少？」 | 指定日期出/入库总件数与记录条数（文本摘要） |
| 4 | **近 N 天统计** | 「最近 7 天出库最多的是什么？」「近 30 天出了多少货？」 | 近 N 天按货品聚合出库量排行 TOP5 + 总量摘要；或按日趋势文本 |
| 5 | **记录检索** | 「最近有哪些出库记录？」「张三领过什么？」「销售部领了什么？」 | 记录列表（时间/领取人/部门/货品×数量/状态），默认最近 10 条；按领取人/部门/用途过滤 |
| 6 | **库存排行** | 「库存最多的前 5 个货品」「库存最少的是哪些？」 | 按当前库存排序的 TOP N 表格（名称+库存） |
| 7 | **趋势** | 「出库趋势怎么样？」「近 7 天出入库趋势」 | 近 7/30 天按日聚合的出库/入库数量文本趋势（可附简易条形） |
| 8 | **帮助/能力说明** | 「你能做什么？」「帮助」 | 返回助手能力清单 + 示例问句（引导 chips） |

> 示例问句集为验收基准，详见 §5。

### 2.3 本地问答引擎设计（engine.js）

**① 输入归一化**：`toLowerCase()` → 去全/半角标点 → 去首尾空白；保留数字与中文。

**② 产品名匹配（优先）**：
- 遍历 `Config.PRODUCTS`，先精确包含匹配（如「冻干精华液 20支装」），再模糊匹配（如「冻干」「面膜」「牛皮纸袋」可命中多个规格）。
- 命中即进入「库存查询」或「记录过滤（含货品名）」意图。
- 无命中才进入通用意图关键词解析。

**③ 意图关键词表（优先级从高到低）**：

| 意图 | 触发关键词（示例） | 数据源 |
|------|-------------------|--------|
| help | 帮助 / 你能做什么 / 怎么用 | 静态文案 |
| low_stock | 低库存 / 快没货 / 不足 / 缺货 / 补货 | `Stock.summarize()` 过滤 `< LOW_STOCK_THRESHOLD` |
| rank | 排行 / 最多 / 最少 / top / 前 N / 榜首 | `Stock.summarize()` 排序 |
| trend | 趋势 / 走势 / 变化 | `Stock.trend(list, 7/30)` |
| in_out_today | 今天 / 昨天 / 今日 / 昨日 + 出库/入库 | `State.list` 按 `time.slice(0,10)` 过滤 |
| in_out_days | 最近 N 天 / 近 N 天 / 本周 / 上月 | `Stock.trend` + 按货品聚合 |
| records | 记录 / 出库记录 / 入库记录 / 领过 / 领了 / 部门 / 用途 / 领取人 | `State.list` 过滤 + 排序 |
| stock | 库存 / 还有多少 / 剩多少 / 有多少 | `Stock.getStock(name)` 或 `summarize` |

**④ 回复构建与渲染**：
- `engine.answer(question) → {type, title, text?, table?, chips?}`，其中 `type ∈ {stock, low_stock, rank, trend, stats, records, help, fallback}`。
- `chat.js` 按 `type` 渲染：`stats` 用文本摘要卡片；`stock/rank/low_stock` 用表格（名称+库存，低库存行红色高亮）；`records` 用记录表；`trend` 用文本列表；均附 2-3 个后续快捷 chips。
- 所有用户/记录数据渲染前必须经 `Util.esc`（XSS 防护，沿用现有约定）。

**⑤ 数据一致性**：所有数值统一取自 `Stock.*` 与 `State.list`（与报表模块同源），**不**另行计算，保证与页面数字一致。

### 2.4 B 部分 API 对接设计（llm.js）

**① API Key 配置**：
- 键名（经 store.js 统一管理，新增）：
  - `outbound_ai_key` — API Key（字符串，存 localStorage，仅本机）
  - `outbound_ai_settings` — `{ model: "deepseek-chat", baseUrl?: "https://api.deepseek.com" }`
- 界面一律用 `type="password"` 输入，回显时掩码（如 `sk-***abc`），提供「清除 Key」。
- **硬约束**：AI Key 不得出现在 `State.list`、CSV 导出、`data/records/*.json`、任何云端同步 payload 中。

**② 默认模型与服务商**：
- 默认：DeepSeek `deepseek-chat`，base `https://api.deepseek.com`（OpenAI 兼容，支持浏览器 CORS 直连）。
- 可选模型下拉：`deepseek-chat` / `deepseek-reasoner`（P1）；预留「自定义 baseUrl + 模型名」输入（P2，供其他 OpenAI 兼容服务）。

**③ 请求格式（OpenAI Chat Completions）**：
```
POST {base}/chat/completions
Headers: Content-Type: application/json
         Authorization: Bearer {API_KEY_PLACEHOLDER}
Body: {
  "model": "deepseek-chat",
  "messages": [
    { "role": "system", "content": "<系统数据上下文（见下）>" },
    { "role": "user",   "content": "<用户问题>" }
  ],
  "stream": false,
  "temperature": 0.7
}
```
- 响应读取：`json.choices[0].message.content`，失败时尝试读取 `json.error.message` 作为错误文案。
- **系统数据上下文（有界注入，控制 token 成本）**：当前库存 TOP15（名称+库存）、低库存货品数、今日出库件数、最近 10 条记录摘要（时间/领取人/货品×数量/状态）、当前本地记录总数。数据全部来自 `Stock.*` / `State.list`，随每次提问动态生成。

**④ 错误处理（Promise 归一化为 `{ok, text?, err?}`）**：
| 场景 | 判定 | 提示 |
|------|------|------|
| Key 无效 | HTTP 401/403 | 「API Key 无效，请在 AI 设置中重新填写」 |
| 限流 | HTTP 429 | 「请求过于频繁，请稍后再试」 |
| 服务端错误 | HTTP 5xx | 「AI 服务暂时不可用，请稍后再试」 |
| 超时 | AbortController 30s | 「响应超时，请重试」 |
| 网络/CORS 失败 | fetch reject | 「网络异常或服务商不支持直连，已退回本地问答」 |

- 统一 `llm.chat(messages, opts) → Promise<{ok, text?, err?}>`；任何失败 → 聊天面板自动回退本地引擎（若问题命中数据意图）或展示错误文案。
- 请求期间输入框禁用 + 显示「正在思考…」；连续点击发送防抖（同一时刻仅一个在途请求）。

**⑤ 成本与隐私说明**：
- 用户自填 Key、自负费用；Key 仅存本机浏览器，不上传任何服务器；系统数据上下文仅在用户提问时随请求发送给所选大模型服务商（**应当**在配置面板明示这一点，用户知情）。

### 2.5 聊天面板 UI / 交互（chat.js）

- **形态**：单例浮动聊天面板。桌面（≥520px）为右下角悬浮卡片（360×520，可拖拽可选）；移动端（<520px）为底部全宽 sheet（高度 ~92vh，顶部圆角），键盘弹出时输入区跟随。
- **消息**：左右气泡（用户右、AI 左），AI 气泡内可按 `type` 渲染表格/列表/文本；AI 首条消息展示能力介绍 + 快捷 chips。
- **快捷 chips**：默认 5 个 —— 「查库存」「今日出库」「低库存」「最近出库记录」「帮我看看报表」（点击即发送）。
- **状态**：发送中显示「正在思考…」气泡 + 输入区禁用；AI 回复完成后自动滚动到底部。
- **清空**：头部 🗑 按钮，二次确认后清空会话（P0 必做；确认框复用 `UI.confirmDialog`）。
- **会话历史**：默认仅内存（P0）；持久化到 `outbound_ai_chat`（P1，上限 50 条，可开关）。
- **关闭**：头部 ✕ 关闭面板；气泡按钮再次打开。

### 2.6 配置面板（AI 设置）

- 入口：聊天面板头部 ⚙ 按钮（弹窗，复用 `UI.Modal`）。
- 字段：
  - 模式提示：无 Key = 本地问答（免费零配置）；有 Key = 对话式 AI（混合模式）。
  - API Key：`type="password"`，占位「粘贴你的 API Key（可选）」；回显掩码。
  - 模型：下拉（deepseek-chat 默认 / deepseek-reasoner P1）。
  - 服务商说明：默认 DeepSeek，浏览器直连（支持 CORS）。
  - 安全说明文案（P0 必须展示）：「🔒 API Key 仅保存在本机浏览器（localStorage），不会上传到服务器，也不会写入代码或仓库；AI 数据查询仅在提问时发送给所选服务商。」
  - 操作：`保存`（校验非空 → 存 store.js）／`清除 Key`（二次确认 → 清除并回退本地模式）／`取消`。
- P1 可选：「测试连接」按钮（发一条最小请求验证 Key 有效性）。

### 2.7 入口集成

| 入口 | 位置 | 实现 |
|------|------|------|
| 落地页浮动气泡 | `#view-landing` 右下角 fixed | `landing.js` render 时注入气泡按钮（或全局浮动组件监听路由），点击打开 chat.js 面板（免登录即可用） |
| 管理后台侧栏菜单项 | `app.js` NAV_ITEMS 末尾新增 `{id:"ai", icon:"box|chat", label:"AI 助手"}` | 路由 `#/app/ai` → `Views.ai.render(el)` 渲染内嵌聊天面板（与浮动面板共用 chat.js，双入口并存） |
| 顶栏浮动按钮 | **不加**（评估结论） | 顶栏已有 ☁️ 同步按钮，再加会拥挤；落地页气泡 + 侧栏菜单已覆盖手机/桌面两场景 |

- **路由改动**：`router.js` KNOWN_MODULES 新增 `ai:1`；`app.js` VIEW_MAP/MODULE_TITLES 新增 `ai`。
- **脚本加载**：`index.html` 在 `sync.js` 之后、`main.js` 之前追加 `js/ai/engine.js`、`js/ai/llm.js`、`js/ai/chat.js`、`js/views/ai.js`；CSS 在 `views.css` 之后追加 `css/ai.css`。**不移动/不删除任何现有脚本**。
- **部署**：deploy.yml 无需改动（`cp -r src/. dist/` 自动带上新文件）；`__GH_TOKEN_PLACEHOLDER__` 内联替换链路不变。

---

## 3. UI 设计稿（ASCII）

### 3.1 落地页浮动气泡（右下角）

```
┌──────────────────────────────────────────────┐
│ 🎨 进销存管理系统                [🖥️ 管理 ➜]    │ ← 彩虹顶栏（现有）
├──────────────────────────────────────────────┤
│                                              │
│   ┌────────────────────────────────────┐     │
│   │  免密出库登记表单（现有，原样保留）     │     │
│   └────────────────────────────────────┘     │
│                                              │
│                                   ┌────────┐ │
│                                   │  💬 🤖 │ │ ← 浮动气泡（彩虹渐变底，白字）
│                                   └────────┘ │
└──────────────────────────────────────────────┘
```

### 3.2 聊天面板（桌面悬浮卡片 / 移动端全宽 sheet）

```
┌─────────────────────────────────┐
│ 🤖 AI 助手              [⚙][🗑][✕] │ ← 头部（设置/清空/关闭）
├─────────────────────────────────┤
│ 💬 你好！我是你的进销存助手，可以    │
│ 问我：                           │
│ ┌─────────┐ ┌─────────┐         │
│ │ 查库存   │ │ 今日出库 │         │ ← 快捷 chips
│ ├─────────┤ ├─────────┤         │
│ │ 低库存   │ │ 最近出库 │         │
│ └─────────┘ └─────────┘         │
│                                 │
│ 你：冻干精华液 还有多少？          │ ← 用户气泡（右）
│                                 │
│ AI：📦 共 3 个规格：              │ ← AI 气泡（左，表格）
│ ┌──────────────────────┐        │
│ │ 货品名称        库存  │        │
│ │ 冻干精华液 20支装  95 │        │
│ │ 冻干精华液 5支装  320 │        │
│ │ 冻干精华液 单支装 222 │        │
│ └──────────────────────┘        │
│                                 │
│  ● 正在思考…                     │ ← 状态气泡
├─────────────────────────────────┤
│ [输入你的问题…            ] [发送] │ ← 输入区
└─────────────────────────────────┘
```

### 3.3 AI 设置弹窗

```
┌──────────────────────────────────┐
│ ⚙ AI 设置                         │
├──────────────────────────────────┤
│ 当前模式：● 本地问答（免费零配置）    │
│          ○ 对话式 AI（填 Key 生效） │
│                                  │
│ API Key：[••••••••••••••]  (password)│
│ 模型：  [deepseek-chat       ▾]   │
│ 服务商：DeepSeek（浏览器直连/CORS） │
│                                  │
│ 🔒 Key 仅保存在本机浏览器           │
│ (localStorage)，不会上传服务器、    │
│ 不会写入代码或仓库。               │
│ 数据查询仅在提问时发送给服务商。    │
│                                  │
│        [清除 Key]   [保存]        │
└──────────────────────────────────┘
```

### 3.4 管理后台侧栏（#/app/ai）

```
┌─────────────┬─────────────────────────────┐
│ 🎨 进销存管理 │ ☁️                  ← 顶栏（现有）│
├─────────────┼─────────────────────────────┤
│ 仪表盘       │                             │
│ 库存查询     │   AI 助手                    │
│ 入库管理     │   ┌─────────────────────┐   │
│ 出库记录     │   │ 💬 聊天气泡…（同 3.2） │   │
│ 入库记录     │   └─────────────────────┘   │
│ 报表统计     │   ┌─────────────────────┐   │
│ 🤖 AI 助手   │   │ [输入…]      [发送]  │   │ ← 新增菜单项（高亮）
├─────────────┼─────────────────────────────┤
│ 就绪｜本地N条 │                             │
└─────────────┴─────────────────────────────┘
```

---

## 4. 保留 / 修改 / 新增 / 删除对比表

| 类别 | 文件/项 | 说明 |
|------|---------|------|
| **保留** | `index.html` 内联 `window.__GH_TOKEN__` | perl 替换 `__GH_TOKEN_PLACEHOLDER__` 链路不变，变量名不得改 |
| 保留 | `data/records/*.json` schema | `{id,time,picker?,dept?,purpose,items:[{name,qty}],photos,affectsStock,type?,status?}` 冻结 |
| 保留 | localStorage 冻结键 | `outbound_records_v2` / `gh_token` / `outbound_dept_history` / `outbound_picker_history` 不改名 |
| 保留 | 全部现有模块与功能 | landing/app/dashboard/out/in/stock/records/report/sync 原样 |
| 保留 | 多彩视觉令牌与 deploy.yml | `--primary #6366F1` / `--grad-main` / `--grad-rainbow` 等；workflow 无需改动 |
| **修改** | `index.html` | 追加 `<link css/ai.css>` 与 4 个 `<script>`（engine/llm/chat/views-ai），不删现有项 |
| 修改 | `js/router/router.js` | `KNOWN_MODULES` 新增 `ai:1` |
| 修改 | `js/views/app.js` | `NAV_ITEMS` 新增「AI 助手」；`VIEW_MAP`/`MODULE_TITLES` 新增 `ai` |
| 修改 | `js/core/config.js` | 新增 AI 常量（`AI_KEY_KEY`/`AI_SETTINGS_KEY`/`AI_DEFAULT_MODEL`/`AI_BASE_URL`/`AI_TIMEOUT_MS` 等） |
| 修改 | `js/core/store.js` | 新增 AI 存取（`loadAiKey/saveAiKey/clearAiKey/loadAiSettings/saveAiSettings`），统一走 Store 封装 |
| 修改 | `js/views/landing.js` | render 时注入右下角浮动气泡（点击开 chat.js 面板） |
| **新增** | `js/ai/engine.js` | 本地问答引擎（意图识别+查询+回复构建） |
| 新增 | `js/ai/llm.js` | B 部分 API 对接（fetch/CORS/错误/超时） |
| 新增 | `js/ai/chat.js` | 聊天面板组件（气泡/chips/输入/状态/清空/设置） |
| 新增 | `js/views/ai.js` | 管理后台 AI 助手页面（#/app/ai） |
| 新增 | `css/ai.css` | AI 面板/气泡/chips/表格样式（沿用设计令牌） |
| 新增 | localStorage 键（经 store.js） | `outbound_ai_key`（API Key）、`outbound_ai_settings`（模型等）、P1 `outbound_ai_chat`（会话历史） |
| **删除** | 无 | 硬约束：本轮零删除，不破坏任何现有功能 |

---

## 5. 验收标准（可测量）

- [ ] **A 离线可用**：断网/无 Key 状态下，8 类查询全部可答（§2.2 示例集逐条命中）。
- [ ] **A 准确率**：示例问句集（≥20 句）意图识别正确率 100%；产品名精确包含匹配 100%、模糊匹配（如「冻干」→ 3 规格）正确。
- [ ] **数据一致性**：AI 回答的库存/总量/排行与报表模块同数据源，数字完全一致。
- [ ] **B 切换**：无 Key → 全部本地；填入有效 Key → 闲聊走 LLM、数据类问题仍本地；Key 无效/超时/断网 → 友好错误并回退，不白屏。
- [ ] **Key 安全**：`outbound_ai_key` 不出现在 `State.list`、CSV、云端 payload、仓库代码中（Code Review 检查）。
- [ ] **无第三方依赖**：新增代码零外部库，仅原生 fetch。
- [ ] **移动端**：375px 宽下聊天面板为全宽 sheet，输入不遮挡，chips 可横向滚动。
- [ ] **回归**：出库登记/库存/记录/报表/云端同步/二维码落地页全部不受影响（手工回归 1 轮）。

---

## 6. 待确认事项（Open Questions）

| # | 问题 | 建议默认 | 影响 |
|---|------|----------|------|
| 1 | 默认模型与 baseUrl | DeepSeek `deepseek-chat` / `https://api.deepseek.com` | B 部分默认配置 |
| 2 | API Key 存储键名 | `outbound_ai_key` / `outbound_ai_settings`（经 store.js） | 键名冻结前需确认 |
| 3 | 快捷 chips 列表 | 查库存 / 今日出库 / 低库存 / 最近出库记录 / 帮我看看报表 | UI 首屏 |
| 4 | 有 Key 时混合策略 | 数据类问题优先本地引擎，闲聊走 LLM | 架构决策，需确认 |
| 5 | 管理后台入口形态 | 侧栏菜单项 + 落地页浮动气泡（顶栏不加） | 入口布局 |
| 6 | 会话历史持久化 | P1：存 `outbound_ai_chat`（上限 50 条，可开关） | 存储键 |
| 7 | 移动端面板形态 | <520px 全宽 sheet，≥520px 悬浮卡片 | 移动适配 |
| 8 | 清空对话二次确认 | 复用 `UI.confirmDialog` | 交互 |
| 9 | 「测试连接」按钮 | P1 可选 | B 部分体验 |
| 10 | 低库存阈值 | 沿用 `Config.LOW_STOCK_THRESHOLD=30` | 数据口径 |
| 11 | 自定义服务商（baseUrl+模型输入） | P2 预留 | 扩展性 |
