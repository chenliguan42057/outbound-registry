# 增量 PRD v5.0 — AI 助手功能大拓展（本地通用问答 + 多轮对话 + 联网搜索 + 办公小工具）

> 版本：v5.0（第五轮增量）｜作者：Alice（PM）｜状态：待架构师评审
> 仓库：chenliguan42057/outbound-registry｜线上：https://chenliguan42057.github.io/outbound-registry/
> 基线：remote main = c15b7d4（第四轮 AI 助手已上线：A 本地数据问答 8 类意图 + B 双服务商 LLM 对话）

---

## 0. 项目信息

- **Language**：中文（与用户需求一致）
- **技术栈**：沿用经典 `<script>` 顺序加载（非 ES Module），**零新增第三方 npm 依赖**；AI/联网调用一律原生 `fetch`；二维码复用现有 `qrcode.js`（全局 `qrcode` 函数，用法同 sync.js：`qrcode(0,"M") → addData → make → createDataURL`）
- **文件结构建议**（架构师可调整，核心是避免 engine.js 膨胀）：
  - `src/js/ai/engine.js` — 意图路由 + 仓库数据问答（**原有 8 类逻辑不动**，新增意图接入路由链）
  - `src/js/ai/knowledge.js` — **新增**：常识知识库条目 / 二十四节气表 / 节日表 / 文案模板 / 快递前缀规则 / 货币参考汇率（纯数据表）
  - `src/js/ai/tools.js` — **新增**：计算器 / 单位换算 / 日期时间 / 日期差 / 金额大写 / 二维码 / 快递识别 工具函数（纯函数，可独立冒烟）
  - `src/js/ai/web.js` — **新增**：联网搜索（天气 Open-Meteo / 百科 Wikipedia / 新闻 Tavily P1），错误归一化 + 超时
  - `src/js/ai/llm.js` — **修改**：多轮上下文构建（buildMessages / chatWithHistory）+ 系统提示词优化
  - `src/js/ai/chat.js` — **修改**：新意图卡片渲染（天气/百科/新闻/二维码/工具）、清空上下文按钮、设置弹窗新增「联网搜索」区块
  - `src/js/core/config.js` / `src/js/core/store.js` — 新增常量与存取（键名经 store.js 统一管理）
  - `src/css/ai.css` — 新增天气卡/百科卡/新闻列表/二维码/工具结果卡片样式
  - `src/index.html` — 追加 `knowledge.js`、`tools.js`、`web.js` 三个脚本，**不删现有项**
- **原始需求复述**：AI 助手从「仓库数据问答 + 有 Key 对话」扩展为四类能力：
  1. 本地通用问答扩展（免费无 Key）：常识问答、计算器、单位换算、日期/时间计算、节气/节日；
  2. 通用对话增强（需 Key）：多轮上下文记忆、回答更连贯；
  3. 联网搜索问答（免费 API 优先，需 Key 的作 P1）：天气、百科、新闻/通用搜索；
  4. 办公小工具（免费无 Key）：日期差、金额大小写转换、条码/二维码生成、快递单号识别、文案模板。

---

## 1. 增量目标

### 1.1 本地通用问答扩展（免费、无需 Key）
1. **离线可答五类新问题**：常识、计算、换算、日期时间、节气节日，无 API 成本、无 Key 依赖。
2. **知识库条目化、易扩展**：常识/节气/节日/模板/快递规则全部独立成数据表（knowledge.js），新增条目不改逻辑。
3. **计算与换算准确**：四则/百分比/乘方/温度/长度/重量等高频场景结果正确（含安全求值，禁用 eval）。
4. **与仓库数据问答互不干扰**：新增意图插入 fallback 之前，仓库数据意图优先级与 v4 完全一致。

### 1.2 通用对话增强（需 API Key）
1. **多轮上下文记忆**：最近 N 轮（用户+AI）作为上下文发给 LLM，支持「先问库存再问那出库记录呢」类指代。
2. **上下文长度可控**：轮数上限 + 总字符预算 + 单条截断，防 token 超限与成本失控。
3. **本地回答进入上下文**：数据类问题仍本地直答（不送 LLM），但回答写入历史，后续指代可引用。
4. **系统提示词优化**：明确角色/能力边界/回复风格，回答更连贯。

### 1.3 联网搜索问答（免费 API 优先）
1. **天气**：城市实时天气（Open-Meteo，免费无 Key，支持 CORS）。
2. **百科**：词条摘要（Wikipedia REST API，免费无 Key，支持 CORS）。
3. **新闻/通用搜索**：P1（Tavily，需 Key，Key 经 store.js 存 localStorage）。
4. **降级不白屏**：无 Key / 断网 / API 失败 → 友好提示 + 本地可答 chips。

### 1.4 办公小工具（免费、无需 Key）
1. **五类开箱即用**：日期差、金额大写、二维码生成、快递识别、文案模板。
2. **复用现有能力**：二维码复用 qrcode.js，零新增依赖。
3. **触发精准、结果可复制**：正则特征强，返回格式清晰，文案/单号结果支持复制。

---

## 2. 变更详述

### 2.1 意图路由总览（engine.js）

**核心原则**：仓库数据意图优先级不变；新增意图插在 fallback 之前；高精度工具意图在「产品名匹配」之前判断，避免「生成二维码 冻干精华液」被误判为库存查询。

```
Engine.answer(q) 路由（自上而下，命中即返回）：
0. normalize（沿用 v4）
1. 工具类高精度意图（正则特征极强，先判，防与产品名冲突）：
   qrcode / express / money_upper / calculator / unit_convert / date_diff / datetime
2. 常识问答 KB 命中（无 dataish 限制；KB 条目关键词避免与数据词重叠）
   └─ 未命中继续
3. 百科/知识引导（以「什么是/是什么/介绍一下/介绍下/百科」触发，且不含数据词）→ wiki(pending)
   └─ 不含触发词则跳过
4. 产品名匹配 → stock / records（仓库数据优先级不变，行为与 v4 完全一致）
5. 仓库数据意图链（v4 不变）：help → low_stock → rank → trend → in_out_today → in_out_days → records → stock
6. 联网意图：weather → news(P1)（返回 pending，由 chat.js 异步调 web.js）
7. fallback（无 Key → 本地兜底帮助；有 Key → LLM 多轮真对话）
```

> 说明 1：**为什么常识问答放第 2 步**——「一年有多少个月」含「有多少」会被 v4 stock 关键词误吞，需在数据链之前命中 KB；KB 条目关键词必须避开产品名与数据词（冒烟用例覆盖）。
> 说明 2：**为什么百科引导放产品匹配之前**——「什么是面膜」「面膜是什么」是百科问法而非库存查询；但「库存最多的是什么」含数据词（库存/最多），跳过百科引导、走数据链 rank，不受影响。
> 说明 3：**pending 机制**——天气/百科/新闻为异步联网，engine 同步返回 `{type:"weather"|"wiki"|"news", pending:true, ...}`，chat.js 见到 pending 调 `Web.*` 异步完成；**仅 `type==="fallback"` 才可能走 LLM**，工具/联网类意图失败只显示错误文案，不送 LLM。

### 2.2 本地通用问答扩展（新意图清单）

#### 2.2.1 常识问答（general_qa，knowledge.js → KB.QA）

| # | 示例问句 | 返回内容 | 规则思路 |
|---|----------|----------|----------|
| 1 | 水的沸点是多少 / 水多少度开 | 标准大气压下水的沸点是 100℃（0℃ 结冰） | 条目关键词命中：/沸点\|多少度开/ |
| 2 | 中国首都是哪里 / 首都是 | 中国的首都是北京 | /首都/ |
| 3 | 一年有多少个月 / 一个月几天 | 一年 12 个月；平年 365 天，闰年 366 天 | /一年\|一个月/ + /多少\|几/ |
| 4 | 光速是多少 | 真空中光速约 30 万公里/秒 | /光速/ |
| 5 | 1公里等于多少米 | 1 公里 = 1000 米 | 单位常识（也可归入单位换算） |

- **组织方式**：`KB.QA = [{k: RegExp, a: string, chips?: string[]}]`，数组顺序即优先级，先命中先返回；内置 **30-60 条**（百科常识/单位常识/基础科学/生活常识）。
- 命中 → `{type:"tool", title:"💡 常识问答", text: a, chips}`；未命中 → 继续路由。

#### 2.2.2 计算器（calculator，tools.js → Tools.calc）

| # | 示例问句 | 返回内容 | 规则思路 |
|---|----------|----------|----------|
| 1 | 123*45+67=？ | 123×45+67 = 5602（附步骤 123×45=5535，+67=5602） | 提取合法表达式 `/^[\d.+\-*/().%^]+$/` 自写解析器求值 |
| 2 | 打八折后是多少 / 100 打八折 | 100 打八折 = 80（100×0.8） | /打(\d+)折/ → ×N/10；无基数时提示「请给原价，如 100 打八折」 |
| 3 | 500 的 20% 是多少 | 500×20% = 100 | /(\d+)\s*(?:的)?\s*(\d+(?:\.\d+)?)%/ → 乘百分比 |
| 4 | 2 的 10 次方 / 3的平方 | 2^10 = 1024；3² = 9 | /(\d+)\s*的\s*(\d+)\s*次方\|平方\|立方/ |
| 5 | 增加 20% 是多少 / 减少 15% | 基数×1.2 / 基数×0.85 | /增加(\d+)%\|减少(\d+)%/（需基数） |

- **安全**：**禁用 eval/new Function**；tokenizer + 递归下降解析（或受限符号表替换），仅允许数字与 `+ - * / ( ) . % ^`；除零/非法表达式 → 友好提示。
- 精度：结果四舍五入保留 6 位并去尾零；负数/小数正常。

#### 2.2.3 单位换算（unit_convert，tools.js → Tools.convertUnit）

| # | 示例问句 | 返回内容 | 规则思路 |
|---|----------|----------|----------|
| 1 | 5公里等于多少米 | 5 公里 = 5000 米 | 数字 + 单位A + 等于/是多少 + 单位B |
| 2 | 100华氏度等于多少摄氏度 | 100°F = 37.8°C | 温度专用公式 °C=(°F−32)×5/9；支持摄氏/华氏/开尔文 |
| 3 | 2斤等于多少克 | 2 斤 = 1000 克 | 重量表：斤/两/公斤/千克/克/吨/磅 |
| 4 | 100美元等于多少人民币 | 约 715 元（参考汇率 1 USD≈7.15 CNY，仅供参考） | 货币表 KB.CURRENCY 静态参考汇率 + 免责说明 |
| 5 | 3升等于多少毫升 | 3 升 = 3000 毫升 | 容积表：升/毫升 |

- 单位表内置（长度/重量/温度/容积/货币），换算因子表驱动；货币为**静态参考汇率**并明示「仅供参考」（P1 可接 open.er-api.com 免费实时汇率，见 §2.5）。
- 未识别单位对 → 友好提示 + 支持列表。

#### 2.2.4 日期/时间计算（datetime，tools.js → Tools.dateCalc）

| # | 示例问句 | 返回内容 | 规则思路 |
|---|----------|----------|----------|
| 1 | 今天几号 / 今天星期几 | 今天是 2026年8月2日，星期日 | 系统时间格式化 + 星期映射 |
| 2 | 3天后是哪天 / 5天前 | 3 天后是 8月5日，星期三 | /(\d+)\s*(天|日)(后\|前)/ 相对计算 |
| 3 | 2026年8月2日是星期几 | 2026年8月2日是星期日 | 解析 `YYYY年M月D日` → Date → 星期 |

#### 2.2.5 节气/节日（solar_term / festival，knowledge.js → KB.SOLAR_TERMS / KB.FESTIVALS）

| # | 示例问句 | 返回内容 | 规则思路 |
|---|----------|----------|----------|
| 1 | 今年立春是哪天 | 2026 年立春是 2月4日（周三） | 按当前年份查表 KB.SOLAR_TERMS |
| 2 | 端午节是几号 | 2026 年端午节是 6月19日（周五） | 农历节日按年份查表 KB.FESTIVALS |
| 3 | 元旦是几号 / 国庆节是哪天 | 元旦每年 1月1日；国庆 10月1日（固定公历可计算） | 固定公历节日直接算 |

- 二十四节气表：内置 **2024-2028 五年**（`{year:{name:"YYYY-MM-DD"}}`）；节日表：公历固定节日（可计算）+ 农历节日（春节/元宵/端午/中秋/重阳/腊八/小年，按年份静态表）。
- 超出年份 → 「暂未收录该年份，可换问今年/明年」。

### 2.3 多轮对话增强（llm.js + chat.js）

#### 2.3.1 上下文窗口策略

- **新增配置**（config.js）：
  - `AI_CONTEXT_ROUNDS: 6` — 保留最近 6 轮（用户+AI 各算一条，最多 12 条消息）
  - `AI_CONTEXT_MAX_CHARS: 3500` — 历史消息总字符预算（按 ~2 字符/token 保守估算 ≈ 1500-2000 token）
  - `AI_CONTEXT_MAX_MSG_CHARS: 300` — 单条历史消息截断上限
- **构建算法** `LLM.buildMessages(history, question)`：
  1. `sysCtx = buildSystemContext()`（沿用 v4 有界数据上下文，不随轮数膨胀）；
  2. 取 `history.slice(-AI_CONTEXT_ROUNDS*2)`，每条 `truncate(text, 300)`；
  3. 角色映射：user→user，ai→**assistant**（本地引擎回答也作为 assistant 消息进入上下文，供后续指代引用）；
  4. 累计超预算 → 从最旧一条开始丢弃，直到满足；
  5. 末尾 push 当前问题（完整不截断）。
- **chat.js 调用**：fallback 且有 Key 时改用 `LLM.chatWithHistory(history, q)`（内部 buildMessages + chat），替换 v4 的 `[system, user]` 直拼。

#### 2.3.2 与仓库数据问答的衔接

- 数据类意图**仍本地直答、不送 LLM**（成本 + 准确），v4 行为不变。
- 数据类回答**写入历史**（v4 已实现 appendAnswer→history），因此「那出库记录呢」（含「记录」→ 本地 records 意图）与「那今天呢」（无数据词 → LLM 用上下文理解）都能衔接。
- P1 增强：指代类短问（「那今天呢」「这个呢」）本地未命中时，若历史最近一条为数据问答，回复附提示「你是想问刚刚的 <主题> 吗？」+ 补问 chips。

#### 2.3.3 系统提示词优化

- 明确角色、能力边界与回复风格，示例：
  > 你是「进销存管理系统」的 AI 助手。系统数据上下文如下，仅作参考。
  > 若用户问题属于库存/出入库统计/记录查询，请优先依据上下文数据回答；上下文无法给出准确数字时，明确说明并建议使用快捷问题（如「查库存」）获取准确数据。
  > 回复使用中文，保持简洁、口语化；涉及数量时带单位。
- 该提示词在 buildSystemContext 内更新（不动数据段结构，兼容 v4 冒烟）。

#### 2.3.4 上下文清空按钮

- 头部保留 🗑「清空对话」= 清空全部历史（内存+持久化）+ 界面重开 intro（**同时清空上下文**）。
- 新增 🧹「清空上下文」：仅清空 LLM 将携带的历史（history 置空 + 清持久化），**界面消息保留展示**，后续 LLM 只带当前问题。说明文案：「清空上下文后 AI 不再记得之前的对话，界面消息仍保留显示。」
- 设置弹窗增加上下文只读提示：当前保留轮数（如「上下文：最近 6 轮」）。

#### 2.3.5 多轮消息展示

- 沿用 v4 气泡（用户右/AI 左/表格/文本/错误/思考中）。
- pending 异步请求细分状态文案：普通「正在思考…」/ 联网「正在联网查询…」/ 天气「正在查询天气…」。
- LLM 多轮回复气泡标题可标注「💬 AI 对话」（沿用），不强加轮数。

### 2.4 联网搜索（web.js）

#### 2.4.1 API 选型对比

| 能力 | 候选 API | 免费 | 需 Key | CORS | 稳定性 | 结论 |
|------|----------|------|--------|------|--------|------|
| 天气 | **Open-Meteo**（geocoding + forecast） | ✅ | 无 | ✅ 明确支持 | 高 | **P0 首选** |
| 天气 | wttr.in（`?format=j1`） | ✅ | 无 | ⚠️ 实现时需验证响应头 | 中（偶不稳定） | P0 备选/兜底 |
| 百科 | **Wikipedia REST v1**（zh.wikipedia.org） | ✅ | 无 | ✅ | 高 | **P0 百科** |
| 新闻/通用搜索 | **Tavily Search** | ✅ 1000 次/月 | ✅ | ✅ | 高 | **P1（需 Key）** |
| 通用搜索 | SerpAPI | ✅ 100 次/月 | ✅ | ✅ | 高 | P1 备选 |
| 通用搜索 | Bing Web Search | ✅ 1000 次/月 | ✅ | ❌ 需服务端代理 | 高 | 不适用 |
| 汇率（货币换算增强） | open.er-api.com | ✅ | 无 | ✅ | 中 | P1 可选 |

- **结论**：P0 = Open-Meteo（天气）+ Wikipedia（百科），**免费无 Key**；P1 = Tavily（新闻/搜索，需 Key）。
- **Key 存储**（P1）：`outbound_search_key`（原始字符串）+ `outbound_search_settings`（`{provider:"tavily", enabledWeather:true, enabledWiki:true, enabledNews:false}`），经 store.js；安全规则与 AI Key 完全一致（只存本机、绝不入代码/仓库/云端）。

#### 2.4.2 触发词与返回渲染

| 意图 | 触发词（示例问句） | 流程 | 返回渲染 |
|------|-------------------|------|----------|
| weather | 「北京天气怎么样」「今天上海气温」「广州会下雨吗」 | 提取城市 → Open-Meteo geocoding（`geocoding-api.open-meteo.com/v1/search?name=北京&count=1&language=zh`）→ forecast（`api.open-meteo.com/v1/forecast?latitude=..&longitude=..&current_weather=true&daily=temperature_2m_max,temperature_2m_min`） | 天气卡：城市 / 当前温度 / 天气描述（代码→中文）/ 最高最低 / 风速 / 湿度；emoji 图标 |
| wiki | 「什么是云计算」「介绍一下大熊猫」「月球百科」 | 提取词条 → `zh.wikipedia.org/api/rest_v1/page/summary/{encodeURIComponent(topic)}` | 百科卡：词条标题 + 摘要（截 300 字）+ 配图（有则显示）+「查看完整词条」链接（target=_blank rel=noopener） |
| news(P1) | 「最新科技新闻」「搜一下 AI 新闻」 | Tavily `POST api.tavily.com/search`（body: {api_key, query, max_results:5}） | 新闻列表 TOP5：标题 + 来源 + 链接 |

- **城市提取**：优先 `/([\u4e00-\u9fa5]{2,6}?)(?:市)?(?:的)?(?:天气|气温|温度|下雨|会不会下雨)/`；内置省会+直辖市+常见城市白名单兜底；支持英文城市名（Beijing）。
- **词条提取**：`/(?:什么是|是什么|介绍一下|介绍下|百科)\s*([\u4e00-\u9fa5a-zA-Z0-9\s]{1,20})/`。
- **超时**：天气/百科 10s、新闻 15s（AbortController）；错误归一化 `Web.request → {ok, text?, err?}`（风格同 llm.js）。
- **降级**：天气/百科失败 → 「天气服务暂时不可用，请稍后再试或检查网络」/「未找到该词条或网络异常」+ 本地 chips；新闻无 Key → 「联网新闻搜索需要配置 API Key：设置 → AI 设置 → 联网搜索」+ 本地 chips；全部失败不白屏、不阻塞其他功能。

#### 2.4.3 设置面板新增「联网搜索」区块

- 开关：启用天气（默认开）/ 启用百科（默认开）/ 启用新闻搜索（默认关）。
- 服务商下拉（仅新闻/搜索需要）：Tavily（默认）/ SerpAPI（P1 备选）。
- API Key：`type="password"`，掩码回显，可清除；占位「粘贴 Tavily API Key（可选，新闻搜索用）」。
- 免费说明：「天气/百科免费无需 Key；新闻搜索需自行申请 Tavily Key（免费额度 1000 次/月）」。
- 安全说明：🔒 与 AI Key 相同规则——仅保存在本机浏览器（localStorage），不会上传服务器，也不会写入代码或仓库。

### 2.5 办公小工具（tools.js）

| 工具 | 触发识别规则（示例问句） | 返回格式 | 复用/实现 |
|------|--------------------------|----------|-----------|
| 日期差 | 「2026-08-01 到 2026-08-10 差几天」`(\d{4}[-/.]\d{1,2}[-/.]\d{1,2})\s*(?:到\|至\|和\|与\|~)\s*(\d{4}[-/.]\d{1,2}[-/.]\d{1,2})` + `差几天\|几天\|相隔\|间隔` | `📅 日期差：2026-08-01 → 2026-08-10 相差 9 天（自然日差）` | 纯 Date 计算 |
| 金额大写 | 「12345.67 大写」「人民币 12345.67 大写」`(\d+(?:\.\d{1,2})?)\s*(?:元)?\s*大写` | `💰 壹万贰仟叁佰肆拾伍元陆角柒分` | 自写 RMB 大写转换（元/角/分/整；0、负数 P1） |
| 二维码 | 「生成二维码 https://x.com」「生成二维码 你好」`生成\s*(?:二维码\|qr码\|条码\|qr)\s*[:：]?\s*(.+)` | AI 气泡内渲染 QR 图片 + 内容文本（可复制）；内容 1-200 字符 | **复用 qrcode.js**：`qrcode(0,"M")→addData→make→createDataURL(5,5)`（同 sync.js） |
| 快递识别 | 「查一下快递 SF1234567890 是哪家」`快递(?:单号\|号)?\s*[:：]?\s*([A-Za-z]{2,4}?\d{9,15}\|[0-9]{10,15})` | `📦 该单号疑似【顺丰速运】（SF 前缀 + 15 位）` | KB.EXPRESS 前缀+长度规则表；未识别 → 友好提示 |
| 文案模板 | 「帮我写个催货短信」「出库单备注模板」「客户通知模板」「写个缺货提醒」`(?:写\|生成\|来个\|帮我写)\s*(.*)(?:短信\|模板\|文案\|通知\|备注)` + 关键词 | 模板文本 + 「可点击复制」提示 | KB.TEMPLATES 内置 8-10 个模板（见下） |

**快递前缀规则表（KB.EXPRESS 示例）**：

| 前缀 | 公司 | 长度约束 |
|------|------|----------|
| SF | 顺丰速运 | SF + 12/15 位数字 |
| YT | 圆通速递 | 12-15 位数字（或 YT 开头） |
| ZTO | 中通快递 | 12-14 位数字 |
| YTO | 韵达快递 | 13 位数字 |
| JD / JDV | 京东物流 | 15 位数字 |
| EMS / EA / EE | 邮政 EMS | 13 位字母+数字 |

**文案模板（KB.TEMPLATES 示例）**：
- 催货短信：「您好，XX 货品当前库存紧张，烦请尽快安排补货，谢谢！——进销存管理」
- 出库单备注：「本单已由 {领取人} 领取 {货品×数量}，用途：{用途}；如有问题请联系管理员。」
- 客户通知：「尊敬的客户，您领取的 {货品} 已于 {日期} 出库，请注意查收/核对。」
- 缺货提醒：「温馨提示：{货品} 库存已低于安全线，请及时补货，避免影响发运。」
- 节日祝福（春节/端午/中秋/元旦等通用模板）。

> 小工具全部本地实现、免费无 Key；后续可扩展更多（如「合同金额大小写」「日期转农历」等，P2）。

### 2.6 聊天面板交互调整（chat.js + ai.css）

- **消息展示**：AI 气泡按 `type` 新增渲染——`tool`（文本结果卡，常识/计算/换算/日期/模板/快递/金额）、`weather`（天气卡）、`wiki`（百科卡）、`news`（新闻列表）、`qr`（二维码图 + 内容）；全部值经 `Util.esc`，链接 `target=_blank rel=noopener`。
- **新能力 chips**：intro 消息增加第二组（次级样式）：「计算」「单位换算」「今天几号」「北京天气」「生成二维码」「快递查询」「写个催货短信」；默认 5 个仓库 chips 保留；移动端横向滚动（现有 .ai-chips 已支持）。
- **上下文清空按钮**：头部新增 🧹（见 §2.3.4）。
- **联网搜索开关**：设置弹窗新增「联网搜索」区块（见 §2.4.3）。
- **思考中状态**：pending 请求细分文案（见 §2.3.5）。

### 2.7 新增/修改配置与存取

**config.js 新增常量**：
- `AI_CONTEXT_ROUNDS: 6`、`AI_CONTEXT_MAX_CHARS: 3500`、`AI_CONTEXT_MAX_MSG_CHARS: 300`
- `SEARCH_KEY_KEY: "outbound_search_key"`、`SEARCH_SETTINGS_KEY: "outbound_search_settings"`
- `WEB_TIMEOUT_MS: 10000`、`WEB_NEWS_TIMEOUT_MS: 15000`
- `AI_EXTRA_CHIPS: ["计算","单位换算","今天几号","北京天气","生成二维码","快递查询","写个催货短信"]`
- `SEARCH_PROVIDERS: [{id:"tavily", label:"Tavily", baseUrl:"https://api.tavily.com"}, {id:"serpapi", label:"SerpAPI", baseUrl:"https://serpapi.com"}]`（P1）
- 天气/百科/搜索端点常量：`WEB_WEATHER_GEO_URL` / `WEB_WEATHER_FORECAST_URL` / `WEB_WIKI_URL` / `WEB_NEWS_URL`

**store.js 新增**：
- `loadSearchKey / saveSearchKey / clearSearchKey`
- `loadSearchSettings / saveSearchSettings`

**index.html 脚本顺序（在 llm.js 之前插入数据/工具，web.js 紧随 llm.js）**：
```
js/ai/knowledge.js
js/ai/tools.js
js/ai/engine.js      （原位置不动）
js/ai/llm.js         （原位置不动）
js/ai/web.js
js/ai/chat.js        （原位置不动）
js/views/ai.js       （原位置不动）
```
（knowledge/tools 在 engine 前加载，engine 可直接引用；web.js 在 llm 后、chat 前。）

---

## 3. UI 设计稿（ASCII）

### 3.1 聊天面板扩展（新能力 chips + 多轮 + 工具/联网卡片）

```
┌─────────────────────────────────┐
│ 🤖 AI 助手            [🧹][🗑][✕] │ ← 新增清空上下文按钮
├─────────────────────────────────┤
│ 💬 你好！我是你的进销存助手，可以  │
│ 问我：                           │
│ ┌─────────┐ ┌─────────┐         │
│ │ 查库存   │ │ 今日出库 │         │ ← 主 chips（保留 5 个）
│ ┌─────────┐ ┌─────────┐         │
│ │ 计算     │ │ 单位换算 │         │ ← 次级 chips（横向滚动）
│ │ 今天几号 │ │ 北京天气 │         │
│ │ 生成二维码│ │ 快递查询 │         │
│ │ 写个催货短信│ …                  │
│                                 │
│ 你：3天后是哪天？                  │ ← 用户气泡
│                                 │
│ AI：📅 日期计算                    │ ← tool 卡片
│ ┌─────────────────────────┐     │
│ │ 3 天后是 8月5日，星期三     │     │
│ └─────────────────────────┘     │
│                                 │
│ 你：北京天气怎么样？               │
│                                 │
│ AI：☀️ 北京天气                  │ ← weather 卡（pending→异步）
│ ┌─────────────────────────┐     │
│ │ 当前 32°C  晴             │     │
│ │ 最高 34° / 最低 26°       │     │
│ │ 风速 3m/s 湿度 45%        │     │
│ └─────────────────────────┘     │
│                                 │
│ 你：什么是云计算？                │
│                                 │
│ AI：🔍 云计算（维基百科）          │ ← wiki 卡
│ ┌─────────────────────────┐     │
│ │ [图] 云计算是通过网络提供…  │     │
│ │ [查看完整词条 ↗]           │     │
│ └─────────────────────────┘     │
│                                 │
│  ● 正在联网查询…                  │ ← pending 状态
├─────────────────────────────────┤
│ [输入你的问题…            ] [发送] │
└─────────────────────────────────┘
```

### 3.2 二维码生成

```
你：生成二维码 https://example.com
AI：
┌─────────────────────────┐
│   ┌─────────────┐       │
│   │ ██ █ █ ███  │       │ ← qrcode.js 渲染（createDataURL）
│   │ ██   ███ ██ │       │
│   │ ███ █ █ █ █ │       │
│   └─────────────┘       │
│  内容：https://example.com │
└─────────────────────────┘
```

### 3.3 AI 设置弹窗（新增「联网搜索」区块）

```
┌──────────────────────────────────┐
│ ⚙ AI 设置                         │
├──────────────────────────────────┤
│ 当前模式：● 本地问答（免费零配置）    │
│          ○ 对话式 AI（填 Key 生效） │
│ API Key：[••••••••••••••]  (password)│
│ 服务商：  [DeepSeek          ▾]   │
│ 模型：    [deepseek-chat    ▾]   │
│ 保存会话历史（最多 50 条） □(✓)     │
│ 上下文：最近 6 轮｜[清空上下文]      │ ← 新增只读提示+按钮
│ ─────────────────────────────── │
│ 🔍 联网搜索（新增区块）             │
│ ☑ 启用天气（免费，无需 Key）        │
│ ☑ 启用百科（免费，无需 Key）        │
│ ☐ 启用新闻搜索（需 Key）            │
│ 服务商：  [Tavily           ▾]   │
│ 搜索 Key：[••••••••••]   (password)│
│ 🔒 Key 仅存本机浏览器，不会上传      │
│ 服务器或写入代码/仓库。             │
│ ─────────────────────────────── │
│ 🔒 AI Key 仅保存在本机浏览器…（原）  │
│        [清除 Key]   [保存]        │
└──────────────────────────────────┘
```

---

## 4. 保留 / 修改 / 新增 / 删除对比表

| 类别 | 文件/项 | 说明 |
|------|---------|------|
| **保留** | `index.html` 内联 `window.__GH_TOKEN__` | perl 替换 `__GH_TOKEN_PLACEHOLDER__` 链路不变，变量名不得改 |
| 保留 | `data/records/*.json` schema | `{id,time,picker?,dept?,purpose,items:[{name,qty}],photos,affectsStock,type?,status?}` 冻结 |
| 保留 | localStorage 冻结键 | `outbound_records_v2` / `gh_token` / `outbound_dept_history` / `outbound_picker_history` 不改名 |
| 保留 | 全部现有模块与功能 | landing/app/dashboard/out/in/stock/records/report/sync 原样 |
| 保留 | AI 仓库数据问答（8 类意图） | engine.js 数据逻辑、detectIntent 数据链、Stock.* 数据源原样 |
| 保留 | 双服务商 LLM（DeepSeek/智谱） | llm.js chat/错误归一化/Key 存储不变 |
| 保留 | 聊天面板 UI 与双入口 | 落地页 FAB + #/app/ai 内嵌；快捷 chips 主组保留 |
| 保留 | 多彩视觉令牌 / deploy.yml / qrcode.js | 沿用；workflow 无需改动 |
| **修改** | `js/ai/engine.js` | 路由链插入新意图（工具→KB→wiki→数据链→联网→fallback）；数据逻辑不动 |
| 修改 | `js/ai/llm.js` | 新增 `buildMessages / chatWithHistory`；系统提示词优化 |
| 修改 | `js/ai/chat.js` | pending 异步渲染；新卡片（tool/weather/wiki/news/qr）；🧹 清空上下文；设置新增联网区块；状态文案细分 |
| 修改 | `js/core/config.js` | 新增上下文/搜索/联网常量（见 §2.7） |
| 修改 | `js/core/store.js` | 新增 `loadSearchKey/saveSearchKey/clearSearchKey/loadSearchSettings/saveSearchSettings` |
| 修改 | `index.html` | 追加 `knowledge.js`、`tools.js`、`web.js` 脚本（顺序见 §2.7），不删现有项 |
| 修改 | `css/ai.css` | 新增 tool/weather/wiki/news/qr 卡片样式、次级 chips 样式 |
| **新增** | `js/ai/knowledge.js` | KB.QA 常识条目 / KB.SOLAR_TERMS / KB.FESTIVALS / KB.TEMPLATES / KB.EXPRESS / KB.CURRENCY |
| 新增 | `js/ai/tools.js` | 计算器/单位换算/日期时间/日期差/金额大写/二维码/快递识别 |
| 新增 | `js/ai/web.js` | 天气(Open-Meteo)/百科(Wikipedia)/新闻(Tavily,P1)，错误归一化+超时 |
| 新增 | localStorage 键（经 store.js） | `outbound_search_key`（搜索 Key）、`outbound_search_settings`（联网开关/服务商） |
| **删除** | 无 | 硬约束：本轮零删除，不破坏任何现有功能 |

---

## 5. 验收标准（可测量）

- [ ] **本地五类新能力离线可用**：断网/无 Key 下常识、计算（≥5 例）、单位换算（长度/重量/温度 ≥5 例）、日期时间（≥5 例）、节气节日（≥5 例）全部可答。
- [ ] **计算安全**：计算器不出现 eval/new Function；非法表达式/除零返回友好提示；`123*45+67=5602`、`100 打八折=80`、`2的10次方=1024` 正确。
- [ ] **工具准确**：`5公里=5000米`、`100华氏度=37.8°C`、`12345.67 大写=壹万贰仟叁佰肆拾伍元陆角柒分`、`2026-08-01到08-10=9天`、`SF1234567890→顺丰` 正确。
- [ ] **二维码复用**：`生成二维码 <内容>` 在 AI 气泡内渲染图片（复用 qrcode.js），内容 1-200 字符，超长友好提示。
- [ ] **路由不破坏 v4**：原 27 句 8 类用例全部仍命中（仓库数据意图优先级不变）；产品名匹配不受新意图影响。
- [ ] **多轮对话**：有 Key 时「帮我看看库存」→「那今天呢」能理解指代（LLM 收到含本地回答的历史）；上下文 ≤6 轮/3500 字符；单条 ≤300 字截断。
- [ ] **联网天气/百科**：`北京天气` 返回天气卡；`什么是云计算` 返回百科卡；断网/API 失败显示错误文案 + 本地 chips，不白屏。
- [ ] **新闻降级（P1）**：无 Key 时提示配置 Tavily Key；有 Key 返回 TOP5 列表。
- [ ] **Key 安全**：`outbound_search_key` / `outbound_ai_key` 均不出现在代码明文/仓库/云端 payload（Code Review 检查）。
- [ ] **零新增依赖**：仅原生 fetch + 现有 qrcode.js。
- [ ] **移动端**：375px 下新增卡片不溢出、次级 chips 横向滚动、输入不遮挡。
- [ ] **回归**：出库登记/库存/记录/报表/云端同步/二维码落地页/AI 现有功能全部不受影响（手工回归 1 轮）；`__GH_TOKEN_PLACEHOLDER__` 注入链路未破坏。

---

## 6. 待确认事项（Open Questions）

| # | 问题 | 建议默认 | 影响 |
|---|------|----------|------|
| 1 | 联网搜索 API 选型 | 天气 Open-Meteo（P0）+ 百科 Wikipedia（P0）免费无 Key；新闻 Tavily（P1 需 Key）；wttr.in 作天气备选（需实现时验证 CORS） | 联网能力边界 |
| 2 | 多轮上下文轮数/预算 | 最近 6 轮、总预算 3500 字符、单条 300 字截断 | token 成本与体验 |
| 3 | 「清空上下文」交互 | 头部 🧹 仅清 LLM 记忆（保留界面消息）；🗑 清空全部（含界面） | 交互复杂度 |
| 4 | 知识库条目范围 | 常识 30-60 条；节气/节日表 2024-2028；模板 8-10 个 | 覆盖面 vs 维护量 |
| 5 | 百科触发与数据词冲突 | 「什么是/是什么/介绍一下」+ 无数据词才走百科；「库存最多的是什么」仍走 rank | 路由准确性 |
| 6 | 货币换算 | P0 静态参考汇率 + 免责；P1 接 open.er-api.com 实时 | 准确性与成本 |
| 7 | 农历节日年份范围 | 2024-2028（与节气一致） | 数据表规模 |
| 8 | 新能力 chips 布局 | intro 第二组次级 chips（横向滚动），主组 5 个保留 | UI 首屏 |
| 9 | 文案模板是否加「复制」按钮 | P0 文本 + 提示可复制；P1 气泡内复制按钮 | 体验 |
| 10 | 指代短问增强（「那今天呢」提示补问） | P1 可选 | 多轮体验 |
| 11 | 搜索 Key 服务商 | 默认 Tavily；SerpAPI 备选 | 设置面板字段 |
| 12 | 文件拆分粒度 | knowledge.js / tools.js / web.js 独立（engine.js 只做路由+仓库问答） | 架构，架构师可调整 |
