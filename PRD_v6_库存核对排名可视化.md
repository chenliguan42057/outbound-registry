# 增量 PRD v6.0 — AI 库存照片核对 + 库存全量排名 + 仪表盘可视化

> 版本：v6.0（第六轮增量）｜作者：Alice（PM）｜状态：待架构师评审
> 仓库：chenliguan42057/outbound-registry｜线上：https://chenliguan42057.github.io/outbound-registry/
> 基线：remote main = d52e982（已含 v5：AI 本地通用问答 + 办公工具 + 多轮 + 联网天气百科、智谱服务商、首页最近提交、30s 自动同步、低库存阈值 95）

---

## 0. 技术约束说明（必须先读）

> ⚠️ **OCR 能力限制（本 PRD 最重要的一条现实约束）**
>
> **浏览器原生没有 OCR 能力**，无法直接识别截图图片里的文字；接入第三方 OCR API（百度/腾讯云等）需要 API Key + 网络成本，违反「零新增第三方依赖」硬约束。因此需求 1 的可行落地方式为：
>
> **用户从库存截图复制文字 → 粘贴到文本框 → AI 解析比对**（文本模式，非图片识别）。
>
> - 交互上保留「📷 库存核对」按钮与「截图」心智，但实际输入是**粘贴文本**，弹窗内给出清晰引导（"从截图复制文字后粘贴"），并对用户明示：**本功能识别的是你粘贴的文字，不是图片本身**。
> - 若未来用户强烈要求「直接传图片识别」，需作为独立需求另行评审（引入 OCR 依赖与 Key 管理，违反当前约束，本版本不做）。
> - 文本解析要做到 robust（容错）：空白行、无关文字、全/半角标点、单位后缀、表格复制粘贴等均可容忍（见 §2.1.2）。

**其余硬约束重申（全部不可破坏）**：
1. 原链接/二维码访问正常（#/ 落地页 + #/app 管理页路由不变）
2. 云端同步链路不变（workflow 注入 GH_TOKEN，data/records/*.json schema 冻结，State.list 结构不变）
3. localStorage 冻结键不改名：`outbound_records_v2` / `gh_token` / `outbound_dept_history` / `outbound_picker_history` / `outbound_ai_*` / `outbound_search_*`
4. `__GH_TOKEN__` 内联与变量名不变（`__GH_TOKEN_PLACEHOLDER__` → perl 替换链路不动）
5. **零新增第三方依赖**：图表一律纯 SVG/CSS，AI 核对纯前端字符串解析，不引入 OCR/图表库
6. 现有 AI 功能 v5 零破坏（本地通用问答 + 办公工具 + 多轮 + 联网天气百科）
7. 现有库存查询/报表/记录管理/入库出库零破坏
8. API Key 安全：任何 Key 不得硬编码入代码，只存 localStorage（沿用 store.js 规则）

---

## 1. 增量目标

### 1.1 AI 库存核对（方向一）
1. **提供「截图库存 vs 系统库存」核对闭环**：用户粘贴截图文字，AI 解析为「货品名 + 数量」清单，与系统当前库存逐项比对，输出 5 类结果（一致 / 数量不符 / 系统多出未提及 / 截图有系统无 / 无法识别）。
2. **解析 robust 且容错**：支持多种分隔符与单位后缀、空白行/无关文字/字符噪音容忍；产品名模糊匹配复用现有 matchProducts 规则，命中歧义时给出候选而非静默错判。
3. **零成本零依赖**：纯前端字符串解析 + 复用现有 Stock.summarize 数据源，与报表数字同源一致；不引入 OCR/API。
4. **入口自然不干扰 v5**：聊天面板新增「📷 库存核对」按钮 + 触发词（核对库存/对一下库存/盘点等），插入意图路由链，不改 v5 既有意图优先级。

### 1.2 库存全量排名（方向二）
1. **全量可见**：库存查询页新增「全部库存排名」，默认展示全部货品（当前库存降序），不再只有 TOP10。
2. **多维度排序**：支持按当前库存（多→少 / 少→多）、货品名称（A→Z / Z→A）切换。
3. **低库存直观高亮**：排名列 + 库存数字 + 低库存状态标签，低库存货品红色高亮。
4. **移动端可用**：沿用 .table-wrap 横向滚动，窄屏不破版。

### 1.3 仪表盘可视化（方向三）
1. **4 个 P0 图表**：货品库存分布环形图、出入库对比柱状图、近 30 天出库热力日历、低库存横向条形图（全部纯 CSS/SVG，零依赖）。
2. **1 个 P1 图表 + 卡片增强**：近期活动时间轴（最近 10 条）；统计卡片新增「今日活跃领取人数」「近 30 天出库总量」。
3. **与现有卡片互补**：4 张 KPI 卡保留（数字总览），图表承载分布/对比/趋势/明细，不重复造轮子。
4. **性能可控**：图表均为 O(n) 单次聚合（n=本地记录数，百~千级），30s 自动同步 refresh 重绘 DOM 规模小，无卡顿风险；预留缓存策略（P1）。

---

## 2. 变更详述

### 2.1 AI 库存核对

#### 2.1.1 入口与交互流

```
触发方式（二选一，互不冲突）：
A. 按钮：聊天面板输入区新增「📷 库存核对」按钮（ai-inputbar 内，发送按钮左侧）
B. 触发词：用户在输入框输入「核对库存 / 比较库存 / 对一下库存 / 检查库存 / 盘点 / 核对」→
   engine 返回引导卡片：「请点击下方 📷 库存核对 按钮，从库存截图复制文字粘贴后开始核对」
   （引导卡片带快捷按钮，点击直接打开粘贴弹窗）

流程：
1. 点击「📷 库存核对」→ UI.Modal 弹窗（含 textarea + 引导文案 + 示例）
2. 用户从截图复制文字 → 粘贴到 textarea → 点击「开始核对」
3. chat.js 调 Engine.checkStock(pastedText)（纯同步，无网络）
4. 弹窗关闭，结果卡片渲染进聊天面板（type="check"）
5. 失败/空输入 → 弹窗内 toast 提示，不关闭弹窗
```

#### 2.1.2 文本解析逻辑（Check.parseText，纯函数，可独立冒烟）

**输入归一化（逐行）**：
- 按 `\n` 或 `\r\n` 分行为数组
- 每行：trim → 去首尾空白与常见噪声字符（`|` `-` `·` 行首装饰符）→ 空行丢弃
- 无关文本行（不含数字或产品名候选）不报错，归入「忽略行」计数（提示用户有几行被忽略，不打断流程）

**行格式匹配（核心正则思路）**：
```
/^(.+?)[\s:：,，、|　\t]+(\d+(?:\.\d+)?)\s*(?:件|个|盒|支|瓶|袋|片|g|ml|瓶装)?\s*$/
（首组 = 货品名候选；第二组 = 数量；单位后缀可选；全/半角冒号、逗号、顿号、空格、制表符均可作分隔）
```
- 数量范围校验：1 ~ 99999，超出视为格式异常
- 无分隔符但整行形如 `货品名95`（无空格）→ 尝试用「产品名最长匹配」切分（先匹配产品名再取尾部数字），P1 容错
- 表格复制粘贴（多列含 `\t`）：以 `\t` 为分隔符的行优先按「最后一列数字」取数量，P1

**产品名模糊匹配（复用 engine.matchProducts）**：
- 命中精确名 → 唯一确定
- 命中多个 fuzzy 候选（如「面膜」→「面膜 5片装」「面膜 1片装」）→ 不静默二选一：
  - 若该行数量可同时与多个候选匹配则**全部列出**（各自比对，标注同源）
  - 更稳妥做法：结果卡片中该行标记「名称不唯一，已列出全部候选」，由用户肉眼确认
- 零命中 → 该行归入「无法识别 / 系统无此货品」（红），保留原文供用户复查

**系统侧数据源**：`Stock.summarize()`（与库存查询/报表同源，数字必一致）

#### 2.1.3 比对结果分类与展示

| 分类 | 判定条件 | 视觉 | 说明 |
|------|----------|------|------|
| ✅ 一致 | 系统有 + 截图有 + 数量相等 | 绿色 | 逐项列出 |
| ⚠️ 数量不符 | 系统有 + 截图有 + 数量不等 | 橙色 | 显示「系统 X vs 截图 Y」（差量） |
| 🔵 截图未提及 | 系统有 + 截图未出现 | 蓝色/灰 | 不一定是错，提醒截图可能不完整 |
| ❌ 系统无此货品 | 截图有 + 系统 PRODUCTS 无 | 红色 | 提示疑似新货品或名称写错 |
| ⚪ 无法识别 | 行格式异常 / 数量超范围 | 灰色 | 保留原文，提示人工复查 |

**汇总行**：`共比对 N 项：✅一致 a ｜ ⚠️数量不符 b ｜ 🔵截图未提及 c ｜ ❌系统无 d ｜ ⚪无法识别 e 行`

**结果卡片结构（type="check"）**：
- 标题：`📋 库存核对结果`
- 汇总行（色块徽章）
- 表格：`货品名称 | 系统库存 | 截图数量 | 结果`（行按分类分组：先一致、再数量不符、再未提及、再系统无、再无法识别）
- chips：`哪些货品低库存？` `今天出了多少货？` `重新核对`

#### 2.1.4 实现位置与复用

| 文件 | 动作 | 说明 |
|------|------|------|
| `src/js/ai/check.js` | **新增** | `Check.parseText(text)` → 解析行清单；`Check.compare(parsed, summary)` → 分类结果；`Check.answer(text)` → 组装 Answer 对象（type:"check", title, table, chips）。纯函数，无 DOM 无网络 |
| `src/js/ai/engine.js` | **修改** | ① 路由链新增 `detectCheck`（触发词正则，插在 Tools 之后、产品名匹配之前，因触发词明确不会与产品名冲突）；命中返回引导卡片（非 pending）。② 暴露 `checkStock(text)` 薄封装 → `Check.answer(text)` |
| `src/js/ai/chat.js` | **修改** | ① ai-inputbar 新增「📷 库存核对」按钮（`data-act="check"`）。② 点击 → 弹窗（textare a + 引导 + 示例 + 开始/取消）→ 提交调 `Engine.checkStock` → `appendAnswer(checkAns)`。③ `appendAnswer` 支持 `type==="check"` 卡片渲染（复用 renderTable，加分类样式） |
| `src/css/ai.css` | **修改** | check 卡片分类色（.ck-ok/.ck-warn/.ck-miss/.ck-unknown）、弹窗引导样式 |
| `src/index.html` | **修改** | 追加 `<script src="js/ai/check.js"></script>`（不删现有项，顺序在 engine.js 之前或之后由架构师定，check.js 无依赖冲突） |

> 说明：check.js 也可并入 tools.js（同为工具函数），但为保持单文件可冒烟、避免 tools.js 继续膨胀，PM 建议独立文件，架构师可调整。

**数据隐私**：粘贴文本仅在本机内存中解析，不写入 localStorage、不入云端 payload、不进入 AI 历史（如需历史见 §5 待确认 1）。

### 2.2 库存全量排名

#### 2.2.1 位置与结构

**位置**：库存查询页 `#/app/stock`。**不修改现有搜索表格**（硬约束零破坏），在现有「库存查询」卡片下方新增一张卡片「全部库存排名」。

```
┌─ 全部库存排名（默认：库存 多→少）────────────────────────────┐
│  排序：[库存 多→少 ▾]                                        │
│  ┌────┬────────────────────────┬────────┬────────┐         │
│  │排名 │ 货品名称               │ 当前库存│ 状态    │         │
│  ├────┼────────────────────────┼────────┼────────┤         │
│  │ 1  │ 精华液 5支装        │ 320    │ 正常    │         │
│  │ 2  │ 精华液 单支装       │ 222    │ 正常    │         │
│  │ …  │ …                      │ …      │ …      │         │
│  │ 19 │ 精华液 30支装       │ 42     │ 低库存  │         │
│  │ 20 │ 华大鹿茸凝时系列礼盒装  │ 4      │ 低库存  │         │
│  └────┴────────────────────────┴────────┴────────┘         │
│  （20 个货品全量展示，移动端 .table-wrap 横向滚动）             │
└────────────────────────────────────────────────────────────┘
```

**排序选项**（原生 `<select>` 或分段按钮）：
- 库存 多→少（默认）
- 库存 少→多
- 货品名 A→Z（中文按 Unicode 码点）
- 货品名 Z→A

**列结构**：排名序号｜货品名称｜当前库存｜状态（低库存/正常 tag）。排名序号随排序结果动态编号（同数量可并列，P1）。

**低库存高亮**：`s.stock < Config.LOW_STOCK_THRESHOLD(95)` → 行 class `low-stock` + 库存数字 `danger-text` + 状态 tag「低库存」，与现有库存表格一致。

**空态/边界**：无货品 → `暂无数据`；名称排序中文按 `localeCompare`（P1）或简单码点（P0 够用，20 个货品内差异可接受）。

#### 2.2.2 实现

| 文件 | 动作 | 说明 |
|------|------|------|
| `src/js/views/stock.js` | **修改** | 新增 rank 区块 DOM + 排序状态变量 + 排序事件 + `renderRank()`。与现有 renderTable 独立，互不干扰；refresh() 时同步重建 |
| `src/css/views.css` | **修改** | 新增 .rank-sort（下拉样式）、排名表格可复用 .table/.table-wrap/.low-stock/.tag，仅少量新增 |

### 2.3 仪表盘可视化增强

#### 2.3.1 页面结构（dashboard.js 新版）

```
┌─ 仪表盘 ──────────────────────────────────────────────────┐
│ [KPI 卡 × 4~6]（保留现有 4 张 + 可选增强 2 张）             │
│ ┌─ 出入库对比 ──┐ ┌─ 库存分布（环形）──┐ ┌─ 近30天出库热力 ─┐ │
│ │ ████ 出库     │ │   ◔ 冻干精华 32%   │ │ ░░▒▒▓░░▒▒▒▓░░  │ │
│ │ ███  入库     │ │  ◔ 面膜 18%        │ │ ▒▒▓▓░░▒▒▒▓▓▒░  │ │
│ └───────────────┘ └───────────────────┘ └────────────────┘ │
│ ┌─ 低库存分布 ─────┐ ┌─ 近期活动时序 ────┐                  │
│ │ ██ 礼盒 4        │ │ ● 10:24 市场部 领 │                  │
│ │ ███ 30支装 42    │ │ ● 09:58 研发 领   │                  │
│ │ ████ 精粹水 52   │ │ ● 昨天 采购 入库  │                  │
│ └──────────────────┘ └──────────────────┘                  │
│ ┌─ 低库存预警列表 ──┐ ┌─ 最近出库 ────────┐（保留现有）     │
│ └──────────────────┘ └──────────────────┘                  │
└────────────────────────────────────────────────────────────┘
```

#### 2.3.2 各图表设计（全部纯 CSS/SVG，零依赖）

| # | 图表 | 数据源 | 视觉实现 | 交互/说明 |
|---|------|--------|----------|-----------|
| 1 | **出入库对比柱状图**（P0） | `State.list` 聚合（affectsStock===true 记录，按 type 汇总 qty） | 两根大柱（CSS flex + height%），出库=紫（#6366F1）、入库=绿（#10B981），柱顶数字 | 总出库 vs 总入库一眼对比；图例 |
| 2 | **货品库存分布环形图**（P0） | `Stock.summarize()`，按类目归组 | 纯 SVG：`<circle>` + `stroke-dasharray` 分段圆弧（donut），中心显示总库存 | 类目归组见 §2.3.3；hover/legend 显示占比（P1 交互，P0 静态 + 图例） |
| 3 | **近 30 天出库热力日历**（P0） | `Stock.trend(State.list, 30)` 的 outQty | CSS grid 方格（6 列 × 5 行 + 月份标签），颜色 5 档按数量分（0 → 淡 → 深紫），title 提示「日期：N 件」 | 仿 GitHub contribution graph；手机端横向滚动 |
| 4 | **低库存横向条形图**（P0） | `Stock.summarize()` filter `<95` sort asc | CSS `.rank-bar` 横向条形（复用 report 样式），红色渐变 | 与「低库存预警列表」同源不同形态：列表保留明细，条形图作可视化（可合并为同一卡片内「条形 + 数字」） |
| 5 | **近期活动时序**（P1） | `State.list` slice(0,10) | CSS 时间轴（左侧圆点 + 竖线 + 右侧内容：时间/部门/货品×数量） | 替代现有纯文本最近出库列表的视觉层级，可并排保留列表 |
| 6 | **KPI 卡增强**（P1） | `State.list` 聚合 | 复用现有 dash-card | 新增「今日活跃领取人数」（今日不重复 picker 数）、「近 30 天出库总量」 |

#### 2.3.3 类目归组方案（图表 2 需要）

20 个货品直接画饼图太碎，PM 建议归组（映射放常量/Config，架构师可改）：

| 大类 | 包含货品 |
|------|----------|
| 精华液 | 精华液 20支/5支/单支/30支装 |
| 面膜 | 面膜 5片装/1片装 |
| 洁面 | 洁面慕斯 150ml/50ml |
| 精粹水 | 精粹水 120ml/30ml |
| 精粹乳 | 精粹乳 80ml/30ml/1ml |
| 精粹霜 | 精粹霜 50g/15g/5g/1g |
| 礼盒 | 华大鹿茸凝时系列礼盒装 |
| 手提袋 | 小鹿牛皮纸袋 大/小 |

> 备选：不做归组，环形图只取 Top8 货品 + 其他聚合（更简单但信息量少）。**归组方式待用户确认（§5-4）**。

#### 2.3.4 性能与刷新

- 每次 `refresh()`（30s 自动同步 / 手动同步后）重建图表 DOM：所有聚合均为 O(n) 单遍，n=本地记录数（千级以内），DOM 总规模 < 5KB，无性能风险。
- 热力日历固定 30 天窗口；环形图 8 段；柱状图 2 柱——均为常量级输出，无需虚拟化/分页。
- P1 缓存：同一数据源（summarize/trend）在同一 tick 内多次调用可缓存结果（如 `var s = Stock.summarize()` 已天然满足，无需额外机制）。

#### 2.3.5 实现

| 文件 | 动作 | 说明 |
|------|------|------|
| `src/js/views/dashboard.js` | **修改** | 新增 renderCompare/renderDonut/renderHeatmap/renderLowBars/renderTimeline（或归入 renderCharts）；render()/refresh() 追加调用；KPI 卡扩展（P1） |
| `src/css/views.css` | **修改** | 新增 .chart-grid/.donut/.heatmap/.timeline/.compare-bar 等样式 |
| `src/js/core/config.js` | **修改** | 新增 `CATEGORY_MAP`（类目归组映射，纯常量；不改现有键值） |

---

## 3. UI 设计稿（ASCII）

### 3.1 AI 库存核对弹窗（chat.js 弹出，复用 UI.Modal）

```
┌─ 📷 库存核对 ─────────────────────────────────────────────┐
│ 📌 从库存截图里复制文字，粘贴到下方（本功能识别文字，不是图片） │
│    每行格式：货品名 数量                                     │
│    示例：                                                    │
│      精华液 20支装 95                                    │
│      面膜 5片装 120件                                        │
│      洁面慕斯 150ml: 85                                      │
│ ┌────────────────────────────────────────────────────────┐ │
│ │精华液 20支装 95                                     │ │
│ │面膜 5片装 120                                           │ │
│ │洁面慕斯 150ml 85件                                      │ │
│ └────────────────────────────────────────────────────────┘ │
│  （已解析 3 行；空输入将提示「请先粘贴文字」）                 │
│                    [取消]          [开始核对]               │
└────────────────────────────────────────────────────────────┘
```

### 3.2 核对结果卡片（渲染进聊天面板，type="check"）

```
🤖 AI 助手
┌──────────────────────────────────────────────────────────┐
│ 📋 库存核对结果（共 5 项）                                 │
│ ✅一致 2 ｜ ⚠️数量不符 1 ｜ 🔵截图未提及 1 ｜ ❌系统无 1     │
│ ┌───────────────┬────────┬────────┬──────────────┐      │
│ │ 货品名称       │ 系统库存│ 截图数量│ 结果          │      │
│ ├───────────────┼────────┼────────┼──────────────┤      │
│ │ 精华液20支 │ 95     │ 95     │ ✅ 一致       │      │
│ │ 洁面慕斯150ml  │ 85     │ 85件   │ ✅ 一致       │      │
│ │ 面膜5片装      │ 139    │ 120    │ ⚠️ 不符(差-19)│      │
│ │ 精粹霜1g   │ 127    │ —      │ 🔵 截图未提及  │      │
│ │ 新货品试用装   │ —      │ 10     │ ❌ 系统无此货品 │      │
│ └───────────────┴────────┴────────┴──────────────┘      │
│  [哪些货品低库存？] [今天出了多少货？] [重新核对]            │
└──────────────────────────────────────────────────────────┘
```

### 3.3 库存全量排名（stock.js 新增区块）

```
┌─ 库存查询 ────────────────────────────────────────────────┐
│  [搜索货品名称…]                                           │
│  货品总数 20 ｜ 低库存（<95）4 项                           │
│  ┌ 现有搜索表格（不动）… ┐                                  │
│ ─────────────────────────────────────────────────────────│
│ 全部库存排名    排序：[库存 多→少 ▾]                        │
│ ┌────┬───────────────────────────┬────────┬─────────┐    │
│ │排名 │ 货品名称                  │ 当前库存 │ 状态     │    │
│ ├────┼───────────────────────────┼────────┼─────────┤    │
│ │ 1  │ 精华液 5支装           │ 320    │ 正常     │    │
│ │ 2  │ 精华液 单支装          │ 222    │ 正常     │    │
│ │ 3  │ 小鹿牛皮纸袋（全系列）大    │ 199    │ 正常     │    │
│ │ …  │ …                         │ …      │ …       │    │
│ │ 19 │ 精华液 30支装          │ 42     │ 🔴低库存 │    │
│ │ 20 │ 华大鹿茸凝时系列礼盒装      │ 4      │ 🔴低库存 │    │
│ └────┴───────────────────────────┴────────┴─────────┘    │
└──────────────────────────────────────────────────────────┘
```

### 3.4 仪表盘图表区（dashboard.js）

```
┌─ 仪表盘 ─────────────────────────────────────────────────────┐
│ [📄本地记录数 1,024] [📤今日出库 36] [📥今日入库 0] [⚠️低库存 4]│
│  [👥今日活跃 8] [📈近30天出库 640]   ← P1 卡片增强             │
│ ┌─ 出入库对比 ──────┐ ┌─ 库存分布 ────────┐ ┌─ 近30天出库热力 ─┐│
│ │ 总出库 1,240 ████ │ │  ◔◔ 冻干精华 32%  │ │ 一二三四五六日    ││
│ │ 总入库 860   ███  │ │ ◔◔ 面膜 18%       │ │ ░░▒▒▓░░░░▒▒▒▓░░  ││
│ │（紫=出 绿=入）    │ │ ◔◔ 精粹霜 14%     │ │ ▒▒▓▓░░▒▒▒▓▓▒░▒▒  ││
│ └──────────────────┘ │ ◔◔ …(8 类)        │ │（颜色深浅=件数）  ││
│                      └───────────────────┘ └──────────────────┘│
│ ┌─ 低库存分布 ─────────┐ ┌─ 近期活动时序 ──────────────┐        │
│ │ 礼盒装 ██ 4          │ │ ● 10:24 市场部 领 面膜×2    │        │
│ │ 30支装 ███ 42        │ │ ● 09:58 研发   领 冻干×5    │        │
│ │ 精粹水 ████ 52       │ │ ● 08:41 采购   入库 洁面×20 │        │
│ │ 精粹乳 █████ 66      │ │ ● 昨天 客服   领 精粹霜×1   │        │
│ └──────────────────────┘ └─────────────────────────────┘        │
│ ┌─ 低库存预警 ──┐ ┌─ 最近出库 ──┐（保留现有两个卡片）            │
│ └───────────────┘ └─────────────┘                               │
└────────────────────────────────────────────────────────────────┘
```

---

## 4. 保留 / 修改 / 新增 / 删除 对比表

### 保留（不动）
| 项 | 说明 |
|----|------|
| `src/js/data/stock.js` | Stock.getStock/summarize/trend 接口不变（check/排名/图表均复用） |
| `src/js/data/records.js` + data/records/*.json | schema 冻结，云端链路不变 |
| `src/js/ai/{llm,knowledge,tools,web}.js` | v5 能力零改动 |
| `src/js/ai/engine.js` 既有 8 类意图 + v5 联网链 | 路由优先级不变，仅在链中插入 check 引导（触发词专属，不抢占既有意图） |
| `src/js/views/{in,out,records,report,sync,landing}.js`、`views/app.js`、`router/router.js`、`auth/*`、`ui/components.js` | 全部零改动 |
| localStorage 冻结键 | `outbound_records_v2`/`gh_token`/`outbound_dept_history`/`outbound_picker_history`/`outbound_ai_*`/`outbound_search_*` 不改名 |
| workflow 部署链路、`__GH_TOKEN__`/`__GH_TOKEN_PLACEHOLDER__` | 不变 |

### 修改
| 文件 | 修改点 |
|------|--------|
| `src/js/ai/engine.js` | 新增 `detectCheck` 触发词 → 引导卡片；暴露 `checkStock(text)` 薄封装 |
| `src/js/ai/chat.js` | ai-inputbar 新增「📷 库存核对」按钮；打开粘贴弹窗；`appendAnswer` 支持 type="check" 卡片 |
| `src/js/views/stock.js` | 新增「全部库存排名」区块 + 排序下拉 + renderRank() |
| `src/js/views/dashboard.js` | 新增 5 个图表渲染函数 + refresh 调用 +（P1）KPI 卡增强 |
| `src/js/core/config.js` | 新增 `CATEGORY_MAP` 常量（仅新增，不改现有键值） |
| `src/css/views.css` | 新增 chart/rank 样式（不删现有类） |
| `src/css/ai.css` | 新增 check 卡片/弹窗样式 |
| `src/index.html` | 追加 `<script src="js/ai/check.js">`（如采纳独立文件方案） |

### 新增
| 文件 | 说明 |
|------|------|
| `src/js/ai/check.js` | 库存核对纯函数：parseText / compare / answer（也可并入 tools.js，架构师定） |

### 删除
| 项 | 说明 |
|----|------|
| 无 | **本版本零删除**，全部为增量，最大限度降低回归风险 |

---

## 5. 待确认事项

1. **AI 核对结果是否保存进会话历史？** 建议：结果卡片与普通 AI 回复一致进入内存历史 + `outbound_ai_chat` 持久化（受 persistChat 开关控制），但**原始粘贴文本不单独持久化**（避免敏感库存数据落盘）。是否可接受？如不可，则核对结果不写历史。
2. **库存排名是否需要分页 / 「只看低库存」过滤？** 当前 20 个货品全量展示无需分页；建议 P1 增加「只看低库存」过滤按钮，是否纳入本轮？
3. **仪表盘 5 个图表具体选哪些？** PM 建议 P0 选 4 个（出入库对比柱、库存分布环形图、30 天出库热力、低库存条形）+ P1（近期活动时序、KPI 卡增强）。是否照此执行，或用户指定其他组合？
4. **库存分布环形图的类目归组方式**：按 §2.3.3 的 8 大类归组，还是「Top8 货品 + 其他」？归组映射表（CATEGORY_MAP）是否放 Config？
5. **核对的数量容差**：建议「数量不等即算不符」（严格一致，P0）；是否需要可配置容差（如 ±1 忽略）？P2 可加设置。
6. **核对支持「行内无数量」（只核货品是否存在）吗？** 建议支持（数量缺省视为不比较数量，仅核对存在性）。
7. **低库存阈值 95 是否继续适用**于排名/图表的低库存高亮？（本 PRD 默认沿用 Config.LOW_STOCK_THRESHOLD）
8. **「📷 库存核对」按钮的放置**：仅在管理后台 AI 面板显示，还是落地页浮动面板也显示？（建议两处共用聊天面板单例，天然同时出现；若仅后台，需 chat.js 加环境判断）
